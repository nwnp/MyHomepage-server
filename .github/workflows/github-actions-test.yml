name: ci pipeline
run-name: ${{ github.actor }} is testing 🚨
on:
  push:
    branches:
      - main
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TEST 🚨"
