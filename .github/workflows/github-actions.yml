name: ci pipeline
run-name: ${{ github.actor }} is testing CI pipeline 🚀
on:
  push:
    branches:
      - main
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Backend CI pipeline start 🚀"
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "second jobs"
