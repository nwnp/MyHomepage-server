type Post {
  id: ID!
  title: String!
  content: String!
  createdAt: String
  imageUrl: String
}

type Query {
  getPostByUserId(id: ID!): [Post]
}

type Mutation {
  register(post: Register!): Post!
}

input Register {
  UserId: ID!
  title: String!
  content: String!
}
